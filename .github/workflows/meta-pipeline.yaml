name: Bootstrap Jenkins infra

on:
  workflow_dispatch:

jobs:
  create-jenkins-instance:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: eu-north-1
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3

      - run: terraform init
        working-directory: terraform-jenkins

      - run: terraform apply -auto-approve -lock-timeout=60s
        working-directory: terraform-jenkins
        env:
          TF_VAR_public_key: ${{ secrets.AWS_PUBLIC_KEY }}

      - name: Get instance IP from Terraform output
        id: tf_output
        run: |
          echo "master_ip=$(terraform output -raw jenkins_master_public_ip)" >> "$GITHUB_OUTPUT"
          echo "agent_build_ip=$(terraform output -raw jenkins_agent_build_private_ip)" >> "$GITHUB_OUTPUT"
          echo "agent_infra_ip=$(terraform output -raw jenkins_agent_infra_private_ip)" >> "$GITHUB_OUTPUT"
        working-directory: terraform-jenkins

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY_B64 }}" | base64 -d > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ steps.tf_output.outputs.master_ip }} >> ~/.ssh/known_hosts

      - name: Wait for SSH to be ready
        run: |
          IP="${{ steps.tf_output.outputs.master_ip }}"
          end=$((SECONDS+120))
          until ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no -o ConnectTimeout=5 ubuntu@$IP "echo ok"; do
            if [ $SECONDS -ge $end ]; then
              echo "Timeout waiting for SSH"
              exit 1
            fi
            echo "Waiting for SSH..."
            sleep 5
          done

      - name: Copy SSH key to master for ProxyJump
        run: |
          scp -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no \
            ~/.ssh/id_rsa \
            ubuntu@${{ steps.tf_output.outputs.master_ip }}:~/.ssh/id_rsa
          ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no \
            ubuntu@${{ steps.tf_output.outputs.master_ip }} \
            "chmod 600 ~/.ssh/id_rsa"

      - name: Generate dynamic inventory
        run: |
          cat <<EOF > jenkins-role/inventory/hosts.yaml
          jenkins_master:
            hosts:
              ${{ steps.tf_output.outputs.master_ip }}:
                ansible_user: ubuntu
                ansible_ssh_private_key_file: ~/.ssh/id_rsa

          jenkins_agent_build:
            hosts:
              ${{ steps.tf_output.outputs.agent_build_ip }}:
                ansible_user: ubuntu
                ansible_ssh_private_key_file: ~/.ssh/id_rsa
                ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o ProxyJump=ubuntu@${{ steps.tf_output.outputs.master_ip }}'

          jenkins_agent_infra:
            hosts:
              ${{ steps.tf_output.outputs.agent_infra_ip }}:
                ansible_user: ubuntu
                ansible_ssh_private_key_file: ~/.ssh/id_rsa
                ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o ProxyJump=ubuntu@${{ steps.tf_output.outputs.master_ip }}'
          EOF
        working-directory: ansible

      - name: Install Ansible
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Run Ansible playbook
        run: |
          ansible-playbook -i jenkins-role/inventory/hosts.yaml jenkins-role/playbook/site.yml \
          --extra-vars "admin_user_pass=${{ secrets.JENKINS_DEFAULT_PASSWORD }}"
        working-directory: ansible